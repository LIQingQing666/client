# 任务：第二轮完善 Flutter 直播带货 App（修复未实现的功能）

## 当前状态

经过第一轮修改，部分功能已实现，但以下功能仍未完成。

---

## 问题清单与修复要求

### 一、视频界面交互问题

**问题 1：右侧按钮（红心、消息、音乐）点击无反应**

**修复要求**：
| 按钮         | 点击行为                                                     |
| ------------ | ------------------------------------------------------------ |
| 红心（点赞） | 调用 `/api/videos/:id/like`，红心变红，点赞数 +1，再次点击取消 |
| 消息         | 跳转到 `/messages` 消息列表页                                |
| 音乐         | 显示音乐详情弹窗或 Toast 提示                                |

**涉及文件**：`lib/pages/feed/video_player_item.dart`

---

**问题 2：视频进度条不可拖动**

**修复要求**：
- 添加可拖动的进度条（Slider）
- 拖动时显示当前播放时间/总时长
- 拖动结束后调用 `controller.seekTo(duration)`

**代码示例**：
```dart
Slider(
  value: _currentPosition.inSeconds.toDouble(),
  max: _totalDuration.inSeconds.toDouble(),
  onChanged: (value) {
    final newPosition = Duration(seconds: value.toInt());
    _controller.seekTo(newPosition);
  },
)
```

**涉及文件**：`lib/widgets/video_player_widget.dart`

------

### 二、直播间商品卡购买功能

**问题**：商品详情页没有"加入购物车"和"立即购买"按钮

**修复要求**：

- 商品详情半屏浮层底部添加两个按钮
- 点击"加入购物车"：调用 `/api/cart/add`，Toast 提示"已添加至购物车"，购物车页面可见
- 点击"立即购买"：跳转到订单确认页（`/order/confirm`），自动填入商品信息

**涉及文件**：`lib/widgets/product_detail_sheet.dart`

------

### 三、购物车勾选结算功能

**问题**：购物车无法勾选商品结算

**修复要求**：

- 每个商品左侧添加 Checkbox
- 底部显示全选、总价、结算按钮
- 勾选后总价自动更新
- 点击结算跳转订单确认页，只传递勾选的商品

**涉及文件**：

- `lib/pages/cart/cart_page.dart`
- `lib/provider/cart_provider.dart`

------

### 四、订单功能完善

**问题 1：订单列表没有按状态筛选**

**修复要求**：

- 顶部添加 TabBar：`全部`、`待支付`、`已支付`、`已完成`
- 根据选中状态调用不同 API

**问题 2：点击订单无反应**

**修复要求**：

- 点击订单跳转到订单详情页（`/order/detail/:orderId`）
- 订单详情页显示：订单号、商品列表（可点击）、收货地址、总价、支付状态

**问题 3：点击订单中的商品无反应**

**修复要求**：

- 点击商品跳转到商品详情页（`/product/:id`）
- 显示商品详细信息

**涉及文件**：

- `lib/pages/order/order_page.dart`
- `lib/pages/order/order_detail_page.dart`（新增）
- `lib/provider/order_provider.dart`

------

### 五、购物车空状态

**问题**：购物车为空时，橙色按钮上面没有文字

**修复要求**：

- 空状态时显示："去逛逛" 或 "前往首页"
- 点击按钮跳转到视频流页面

**代码示例**：

if (cartItems.isEmpty) {
  return Center(
    child: Column(
      mainAxisAlignment: MainAxisAlignment.center,
      children: [
        Icon(Icons.shopping_cart_outlined, size: 80, color: Colors.grey),
        const SizedBox(height: 16),
        const Text('购物车空空如也'),
        const SizedBox(height: 16),
        ElevatedButton(
          onPressed: () => context.go('/feed'),
          child: const Text('去逛逛'),
        ),
      ],
    ),
  );
}

**涉及文件**：`lib/pages/cart/cart_page.dart`

------

### 六、头像和昵称修改回显问题

**问题**：修改头像和昵称后，我的页面没有更新

**修复要求**：

- 保存后调用更新 API
- 更新本地存储的用户信息
- 使用 Riverpod 刷新我的页面 UI

**代码示例**：

// 保存后
await userApi.updateUser(newName, newAvatar);
await storageService.saveUser(newUser);
ref.invalidate(userProvider);  // 刷新状态
context.go('/mine');  // 返回我的页面

**及文件**：

- `lib/pages/mine/edit_profile_page.dart`
- `lib/provider/user_provider.dart`
- `lib/pages/mine/mine_page.dart`

------

### 七、商家后台交互

**问题**：数据看板中"视频 GMV 排行"和"品类分布"点击无反应

**修复要求**：

| 模块          | 点击行为                                        |
| :------------ | :---------------------------------------------- |
| 视频 GMV 排行 | 跳转到视频排行详情页（显示所有视频及 GMV 数据） |
| 品类分布      | 跳转到品类分析页（显示各品类销售占比图表）      |

**涉及文件**：

- `lib/pages/admin/admin_dashboard_page.dart`
- `lib/pages/admin/gmv_ranking_page.dart`（新增）
- `lib/pages/admin/category_analysis_page.dart`（新增）

------

## 新增文件清单

| 文件                                          | 功能            |
| :-------------------------------------------- | :-------------- |
| `lib/pages/order/order_detail_page.dart`      | 订单详情页      |
| `lib/pages/admin/gmv_ranking_page.dart`       | 视频 GMV 排行页 |
| `lib/pages/admin/category_analysis_page.dart` | 品类分析页      |

------

## 修改文件清单

| 文件                                        | 修改内容                    |
| :------------------------------------------ | :-------------------------- |
| `lib/pages/feed/video_player_item.dart`     | 点赞、消息、音乐按钮        |
| `lib/widgets/video_player_widget.dart`      | 可拖动进度条                |
| `lib/widgets/product_detail_sheet.dart`     | 添加购物车和购买按钮        |
| `lib/pages/cart/cart_page.dart`             | 勾选结算 + 空状态           |
| `lib/provider/cart_provider.dart`           | 选中状态管理                |
| `lib/pages/order/order_page.dart`           | Tab 筛选 + 点击跳转详情     |
| `lib/provider/order_provider.dart`          | 状态筛选逻辑                |
| `lib/pages/mine/edit_profile_page.dart`     | 保存后回显                  |
| `lib/pages/mine/mine_page.dart`             | 刷新用户信息                |
| `lib/provider/user_provider.dart`           | 用户状态管理                |
| `lib/pages/admin/admin_dashboard_page.dart` | 视频 GMV 排行和品类分布点击 |
| `lib/core/app_router.dart`                  | 添加新路由                  |

------

## 路由配置

// app_router.dart 中添加

// 订单详情
GoRoute(
  path: '/order/detail/:orderId',
  name: 'orderDetail',
  builder: (context, state) => OrderDetailPage(orderId: state.pathParameters['orderId']!),
),

// 商品详情
GoRoute(
  path: '/product/:id',
  name: 'productDetail',
  builder: (context, state) => ProductDetailPage(productId: state.pathParameters['id']!),
),

// 视频 GMV 排行
GoRoute(
  path: '/admin/gmv-ranking',
  name: 'gmvRanking',
  builder: (context, state) => const GmvRankingPage(),
),

// 品类分析
GoRoute(
  path: '/admin/category-analysis',
  name: 'categoryAnalysis',
  builder: (context, state) => const CategoryAnalysisPage(),
),

## 新增依赖

无新增依赖。

------

## 验收标准

### 视频界面

- 点赞按钮可点击，红心变红
- 消息按钮跳转消息列表
- 音乐按钮有响应
- 进度条可拖动，视频跳转到指定位置

### 直播间商品卡

- 商品详情页有"加入购物车"和"立即购买"按钮
- 加入购物车后购物车有数据
- 立即购买跳转订单确认页

### 购物车

- 商品可勾选，总价实时更新
- 全选功能正常
- 空状态显示"去逛逛"按钮

### 订单

- 顶部 Tab 可切换筛选
- 点击订单跳转详情页
- 点击订单中商品跳转商品详情页
- 待支付订单有"去支付"按钮

### 我的页面

- 修改头像后我的页面显示新头像
- 修改昵称后我的页面显示新昵称

### 商家后台

- 点击"视频 GMV 排行"跳转详情页
- 点击"品类分布"跳转分析页

------

## 技术栈

- 移动端：Flutter + Riverpod + video_player
- 后端：Node.js + Fastify + better-sqlite3

请按优先级依次修复，每完成一项告诉我。