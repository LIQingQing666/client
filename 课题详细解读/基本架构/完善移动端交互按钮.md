# 任务：完善移动端直播/短视频带货 App 交互功能

## 当前状态

App 核心功能已实现，UI 布局完整，但以下交互功能未完成。

---

## 需要补充的交互功能

### 1. 视频界面交互

**文件位置**：`lib/pages/feed/feed_page.dart` 和相关组件

| 功能               | 实现要求                                                     |
| ------------------ | ------------------------------------------------------------ |
| 推荐/关注 Tab 切换 | 使用 `TabBar` + `TabBarView`，点击"推荐"加载 `/api/videos/recommend`，点击"关注"加载 `/api/videos/follow`（后端需增加这两个接口） |
| 关注按钮           | 点击调用 `/api/users/:userId/follow`，成功后按钮变为"已关注"，状态存储到后端，使用 Riverpod 管理关注状态 |
| 消息按钮           | 跳转到 `/message` 页面，显示消息列表（Mock 数据即可）        |
| 分享按钮           | 使用 `share_plus` 插件调用系统分享面板，分享当前视频标题和链接 |
| 搜索按钮           | 跳转到 `/search` 页面，输入关键词后调用 `/api/videos/search?keyword=xxx` |

### 2. 直播界面交互

**文件位置**：`lib/pages/live/live_room_page.dart`

| 功能     | 实现要求                                                     |
| -------- | ------------------------------------------------------------ |
| 关注按钮 | 同视频界面，调用 `/api/users/:userId/follow`                 |
| 人头数字 | 点击显示 `showModalBottomSheet` 展示在线观众列表（数据来自 WebSocket 的 `online_count` 事件） |
| 分享按钮 | 同视频界面，使用 `share_plus`                                |
| 礼物按钮 | 点击弹出 `showModalBottomSheet` 展示礼物列表，选择后通过 WebSocket 发送 `send_gift` 事件，后端响应 `gift_sent` 事件，显示礼物动画 |

### 3. 我的界面交互

**文件位置**：`lib/pages/mine/mine_page.dart`

| 功能     | 实现要求                                                     |
| -------- | ------------------------------------------------------------ |
| 设置按钮 | 跳转到 `/settings` 页面，包含：退出登录（清除 token）、清理缓存（调用 Hive 清除）、关于我们 |

### 4. 商家后台交互

**文件位置**：`lib/pages/admin/` 目录

| 功能                    | 实现要求                                                     |
| ----------------------- | ------------------------------------------------------------ |
| 数据看板 - 热门商品点击 | 点击商品跳转到 `/admin/product/:id`，显示商品详情（名称、价格、库存、销量、编辑表单） |
| 商品管理 - 点击具体商品 | 同上一行，跳转到商品详情/编辑页                              |
| 视频管理 - 点击具体视频 | 跳转到 `/admin/video/:id`，显示视频详情（标题、封面、URL、关联商品、状态切换） |

---

## 后端需要新增的接口

| 方法   | 路径                           | 功能                 |
| ------ | ------------------------------ | -------------------- |
| GET    | `/api/videos/recommend`        | 推荐视频列表         |
| GET    | `/api/videos/follow`           | 关注用户的视频列表   |
| GET    | `/api/videos/search?keyword=`  | 视频搜索             |
| POST   | `/api/users/:userId/follow`    | 关注用户             |
| DELETE | `/api/users/:userId/follow`    | 取消关注             |
| GET    | `/api/users/:userId/followers` | 粉丝列表             |
| GET    | `/api/users/:userId/following` | 关注列表             |
| POST   | `/api/live/gift`               | 发送礼物（扣减积分） |

---

## 前端新增文件

| 文件                                       | 功能            |
| ------------------------------------------ | --------------- |
| `lib/pages/message/message_page.dart`      | 消息列表页面    |
| `lib/pages/search/search_page.dart`        | 搜索页面        |
| `lib/pages/live/gift_panel.dart`           | 礼物面板组件    |
| `lib/pages/live/audience_list.dart`        | 在线观众列表    |
| `lib/pages/admin/product_detail_page.dart` | 商品详情/编辑页 |
| `lib/pages/admin/video_detail_page.dart`   | 视频详情/编辑页 |
| `lib/pages/settings/settings_page.dart`    | 设置页面        |

---

## 前端修改文件

| 文件                                           | 修改内容                   |
| ---------------------------------------------- | -------------------------- |
| `lib/pages/feed/feed_page.dart`                | Tab 切换、关注逻辑         |
| `lib/pages/feed/video_player_item.dart`        | 分享按钮回调               |
| `lib/pages/live/live_room_page.dart`           | 关注、观众列表、分享、礼物 |
| `lib/pages/mine/mine_page.dart`                | 设置跳转                   |
| `lib/pages/admin/admin_dashboard_page.dart`    | 热门商品点击跳转           |
| `lib/pages/admin/product_management_page.dart` | 商品点击跳转               |
| `lib/pages/admin/video_management_page.dart`   | 视频点击跳转               |
| `lib/core/app_router.dart`                     | 添加新路由                 |
| `lib/provider/follow_provider.dart`            | 关注状态管理（新增）       |
| `lib/provider/gift_provider.dart`              | 礼物积分管理（新增）       |

---

## 路由配置

在 `app_router.dart` 中添加：

```dart
// 消息页
GoRoute(
  path: '/message',
  name: 'message',
  builder: (context, state) => const MessagePage(),
),

// 搜索页
GoRoute(
  path: '/search',
  name: 'search',
  builder: (context, state) => const SearchPage(),
),

// 设置页
GoRoute(
  path: '/settings',
  name: 'settings',
  builder: (context, state) => const SettingsPage(),
),

// 商品详情/编辑页
GoRoute(
  path: '/admin/product/:id',
  name: 'adminProductDetail',
  builder: (context, state) {
    final id = state.pathParameters['id']!;
    return ProductDetailPage(productId: id);
  },
),

// 视频详情/编辑页
GoRoute(
  path: '/admin/video/:id',
  name: 'adminVideoDetail',
  builder: (context, state) {
    final id = state.pathParameters['id']!;
    return VideoDetailPage(videoId: id);
  },
),
```

## 新增依赖

在 `pubspec.yaml` 中添加：

dependencies:
  share_plus: ^10.0.0   # 分享功能

运行 `flutter pub get`。

## WebSocket 礼物事件

### 客户端发送

{
  "event": "send_gift",
  "data": {
    "roomId": "live_123",
    "giftId": "gift_heart",
    "giftName": "❤️",
    "price": 1
  }
}

### 服务端响应

{
  "event": "gift_sent",
  "data": {
    "userId": "u1",
    "userName": "测试用户",
    "giftName": "❤️",
    "animation": "heart"
  }
}

## Mock 数据参考

### 礼物列表

final List<Gift> gifts = [
  Gift(id: 'g1', name: '❤️', price: 1, icon: '❤️'),
  Gift(id: 'g2', name: '🎁', price: 5, icon: '🎁'),
  Gift(id: 'g3', name: '🚀', price: 10, icon: '🚀'),
  Gift(id: 'g4', name: '🎉', price: 20, icon: '🎉'),
  Gift(id: 'g5', name: '👑', price: 50, icon: '👑'),
];

### 在线观众

final List<Audience> audiences = [
  Audience(userId: 'u2', name: '小明', avatar: '...'),
  Audience(userId: 'u3', name: '小红', avatar: '...'),
];

## 验收标准

- 推荐/关注 Tab 可切换，加载不同视频列表
- 关注按钮可点击，状态变化并存储
- 消息、分享、搜索按钮有响应
- 直播界面：关注、观众列表、分享、礼物功能正常
- 我的界面：设置跳转正常
- 商家后台：热门商品、商品列表、视频列表点击跳转到详情页
- 礼物发送后显示动画效果
- 所有操作有 Toast 提示反馈

------

## 技术栈

- 移动端：Flutter + Riverpod + video_player + share_plus
- 后端：Node.js + Fastify + better-sqlite3 + [socket.io](https://socket.io/)

请按优先级依次修复，每完成一项告诉我。

---

## 修正内容总结

| 修正项               | 说明                 |
| -------------------- | -------------------- |
| ✅ 后端接口           | 明确了需要新增的 API |
| ✅ 状态管理           | 关注状态用 Riverpod  |
| ✅ 商品/视频详情页    | 明确了跳转后页面内容 |
| ✅ 礼物积分扣减       | 需要后端扣减积分     |
| ✅ 搜索功能           | 需要后端搜索 API     |
| ✅ Toast 提示         | 操作反馈             |
| ✅ WebSocket 礼物事件 | 明确了事件格式       |

---

