# 任务：修复移动端直播/短视频带货 App 的数据连通问题

## 当前状态

Flutter 移动端代码已完成，后端 Node.js 服务已启动，但 App 运行时出现以下问题：

1. 视频流页面显示"网络异常，请检查连接"、"加载失败"
2. 购物车页面显示"购物车是空的"（实际后端有数据）
3. 订单页面显示"暂无订单"
4. 个人中心显示"即将实现"

## 需要完成的任务

### 1. 修复后端数据连通（最高优先级）

检查并修复以下问题：

**后端目录**：`apps/server/`

- 确保 `/api/videos` 返回有效的视频数据（包含 id、url、coverUrl、title 等字段）
- 确保视频 URL 是有效的可播放地址（先用测试视频替换）
- 确保 `/api/cart/:userId` 返回购物车数据
- 确保 `/api/orders/:userId` 返回订单数据
- 检查 WebSocket 连接是否正常

### 2. 使用本地测试视频

由于后端 mock 的视频 URL 可能无效，请：

- 在 `apps/server/src/db/seed.ts` 中，将视频 URL 替换为可用的测试视频地址
- 推荐使用以下公开测试视频：
  - `https://flutter.github.io/assets-for-api-docs/assets/videos/butterfly.mp4`
  - `https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/ForBiggerBlazes.mp4`
  - `https://commondatastorage.googleapis.com/gtv-videos-bucket/sample/SubaruOutbackOnTheRoadAndInTheField.mp4`

### 3. 添加模拟购物车数据

在 seed 数据中，为测试用户（userId=1）添加购物车数据：
- 至少 2-3 个商品
- 包含商品名称、价格、数量、图片

### 4. 添加模拟订单数据

为测试用户（userId=1）添加订单数据：
- 至少 1-2 个订单
- 不同状态：待支付、已支付、已完成

### 5. 实现简单的个人中心页面

`pages/mine/mine_page.dart`：
- 显示用户头像、昵称（硬编码）
- 显示订单入口
- 显示设置入口

### 6. 实现商家/运营后台（基础版）



创建 `pages/admin/admin_page.dart`：
- 简单的商品列表管理（展示商品、上下架）
- 视频列表管理（展示视频、发布/下架）
- 可以使用 Tab 切换

添加路由 `/admin` 入口（个人中心添加"商家后台"按钮）

### 7. 修复网络错误提示

检查 Dio 配置：
- 确保 `baseUrl` 指向正确的后端地址（`http://192.168.50.174:3000/api`）
- 添加网络超时重试机制
- 添加更友好的错误提示

## 验收标准

- [ ] 打开 App 能看到视频列表，视频可以播放
- [ ] 上下滑动切换视频流畅
- [ ] 购物车页面有数据，可以修改数量、删除
- [ ] 订单页面能看到订单列表
- [ ] 个人中心有内容，不是"即将实现"
- [ ] 商家后台可以查看商品和视频列表
- [ ] 所有网络请求不再报错

## 技术栈

- 移动端：Flutter + Riverpod + video_player
- 后端：Node.js + Fastify + better-sqlite3

请按优先级依次修复，每完成一项告诉我。